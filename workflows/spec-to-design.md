# Spec to design
`to-spec` → `page-inventory` → `information-architecture` → `user-flow` → `design-system-reader` → `page-layout-designer` → `design-validator`; PASS then `figma-screen-builder`, FAIL returns to layout.
