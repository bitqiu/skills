# Creating a Custom Skill

1. Search existing skills and prove a gap.
2. Write `.skill-drafts/<name>.md`.
3. Invoke `skill-creator` to create exactly one skill.
4. Review through `skill-creator`, run boundary tests, and update through it if needed.
5. Register and validate. Never manually author `skills/foo/SKILL.md` when `skill-creator` is available.
